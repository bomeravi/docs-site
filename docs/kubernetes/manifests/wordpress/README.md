# WordPress Kubernetes Example

This example includes a WordPress deployment and a MySQL deployment/service. For production use, replace the PVC/StorageClass, move the database to a managed service, and secure secrets.
